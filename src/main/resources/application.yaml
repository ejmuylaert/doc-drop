spring.datasource:
  url: jdbc:postgresql://localhost:5432/docdrop
  username: postgres
  password: postgres

spring.mvc:
  static-path-pattern: /static/**

upload:
  path: "./upload"

storage:
  path: "./upload"

spring:
  jpa:
    show-sql: true

logging:
  level:
    org.springframework.transaction: TRACE